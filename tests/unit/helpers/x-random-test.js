import {
  xRandom
} from 'cmc-virus-chart/helpers/x-random';

module('XRandomHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = xRandom(42);
  ok(result);
});
